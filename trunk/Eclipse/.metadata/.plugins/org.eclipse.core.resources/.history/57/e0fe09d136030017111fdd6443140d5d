package ch.mitti.grafik;

import java.awt.Canvas;
import java.awt.Color;
import java.awt.Graphics;

public class Dice extends Canvas {
	
	private int count;
	
	public Dice(int count){
		this.count = count;
		this.setSize(100,100);
		this.setBackground(Color.BLUE);
	}
	
	public int getCount(){
		return count;
	}
	
	@Override
	public void paint(Graphics g){
		g.setColor(Color.WHITE);
		int eyeSize = this.getHeight()/5;
		int topY = this.getHeight()/5;
		int bottomY = this.getHeight()/5+eyeSize;
		int leftX = this.getWidth()/5;
		int rightX = this.getWidth()/5+eyeSize;
		int center = this.getHeight()/5-(eyeSize/2);
		
		
		if(count<1||count>6) return;
		else{
			switch(count){
			case 1:
				
				break;
			
			case 2:
				
				break;
			
			case 3:
				
				break;
				
			case 4:
			
				break;
			
			case 5:
				
				break;
				
			case 6:
				
				break;
			}
		}
		
	}
}
